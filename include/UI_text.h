#ifndef UI_TEXT_H
#define UI_TEXT_H
#include "Component.h"

class UI_text : public Component {
    private:

    public:
       // UI_text(GameObject* associated, Vect2 pos);
       // UI_text(GameObject* associated, Vect2 pos, Sp);
       // ~UI_text();
};


#endif